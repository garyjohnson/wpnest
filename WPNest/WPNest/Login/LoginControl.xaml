<UserControl x:Class="WPNest.Login.LoginControl" 
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:WPNest="clr-namespace:WPNest" mc:Ignorable="d" 
			 FontFamily="{StaticResource PhoneFontFamilyNormal}" 
			 FontSize="{StaticResource PhoneFontSizeNormal}" 
			 Foreground="{StaticResource PhoneForegroundBrush}" 
			 d:DesignHeight="480" d:DesignWidth="480">

	<d:DataContext>
		<WPNest:MainPageViewModel />
	</d:DataContext>

	<Grid x:Name="ContentPanel" Margin="12,0,12,0">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBox x:Name="username" Grid.Row="0" Text="{Binding Path=UserName, Mode=TwoWay}" />
		<PasswordBox x:Name="password" Grid.Row="1" Password="{Binding Path=Password, Mode=TwoWay}" />
		<Button Click="OnLoginPressed" Grid.Row="2">Login</Button>
	</Grid>

</UserControl>